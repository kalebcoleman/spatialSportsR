% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/postgres-sync.R
\name{sync_sqlite_to_postgres}
\alias{sync_sqlite_to_postgres}
\title{Sync tables from SQLite to PostgreSQL via chunked UPSERT}
\usage{
sync_sqlite_to_postgres(
  sqlite_path = NULL,
  pg_con = NULL,
  tables = NULL,
  conflict_keys = NULL,
  seasons = NULL,
  season_type = NULL,
  chunk_size = 50000L,
  verbose = TRUE
)
}
\arguments{
\item{sqlite_path}{Path to the SQLite database file.}

\item{pg_con}{A DBI connection to PostgreSQL (or \code{NULL} to auto-connect via
environment variables).}

\item{tables}{Character vector of table names to sync. Defaults to the 17
core pipeline tables.}

\item{conflict_keys}{Named list mapping table names to character vectors of
conflict (unique key) columns. Defaults to \code{.default_conflict_keys()}.}

\item{seasons}{Optional character/integer vector of season values to filter
on. \code{NULL} means sync all rows (full backfill).}

\item{season_type}{Optional season type filter (e.g., \code{"regular"},
\code{"Regular Season"}).}

\item{chunk_size}{Number of rows to read/write per batch (default 50000).}

\item{verbose}{Print progress messages (default \code{TRUE}).}
}
\value{
A data frame of sync results (table, rows_synced, elapsed_secs,
error).
}
\description{
Reads tables from a SQLite database and upserts them into PostgreSQL.
Creates tables in Postgres if they don't exist, adds new columns if the
schema has evolved, and never drops tables or existing indexes.
}
