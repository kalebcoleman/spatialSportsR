% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/postgres-sync.R
\name{sync_current_season}
\alias{sync_current_season}
\title{Sync the current season from SQLite to PostgreSQL}
\usage{
sync_current_season(
  sqlite_path = NULL,
  pg_con = NULL,
  season,
  season_type = NULL,
  tables = NULL,
  conflict_keys = NULL,
  chunk_size = 50000L,
  verbose = TRUE
)
}
\arguments{
\item{sqlite_path}{Path to the SQLite database file.}

\item{pg_con}{A DBI connection to PostgreSQL (or \code{NULL} to auto-connect via
environment variables).}

\item{season}{Season identifier (e.g., \code{"2025-26"} or \code{2026}).}

\item{season_type}{Season type filter (e.g., \code{"regular"}).}

\item{tables}{Character vector of table names to sync. Defaults to the 17
core pipeline tables.}

\item{conflict_keys}{Named list mapping table names to character vectors of
conflict (unique key) columns. Defaults to \code{.default_conflict_keys()}.}

\item{chunk_size}{Number of rows to read/write per batch (default 50000).}

\item{verbose}{Print progress messages (default \code{TRUE}).}
}
\value{
A data frame of sync results (invisibly).
}
\description{
A convenience wrapper around \code{\link[=sync_sqlite_to_postgres]{sync_sqlite_to_postgres()}} that filters to a
single season.
}
